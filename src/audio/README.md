# src/audio/

Sound effects (SFX) patterns.

Put here:
- "move" beep pattern
- "invalid move" error pattern
- "win" pattern
- "tie" pattern
- "switch game" pattern

This should call `drivers/buzzer.*` to actually produce tones.
