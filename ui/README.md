# src/ui/

Rendering layer.

Put here:
- Drawing the tic-tac-toe grid
- Drawing X/O marks
- Drawing cursor (selected cell)
- Showing which game is active (A or B)
- Optional animations (blink cursor, win highlight)

This should call `drivers/display.*` but not touch joystick/buzzer directly.
